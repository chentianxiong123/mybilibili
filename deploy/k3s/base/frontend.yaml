# 前端：web(Nuxt SSR,node:3200) + admin(Vite SPA,nginx:3100)
---
apiVersion: apps/v1
kind: Deployment
metadata: {name: web, namespace: mybilibili}
spec:
  replicas: 1
  selector: {matchLabels: {app: web}}
  template:
    metadata: {labels: {app: web}}
    spec:
      containers:
        - name: web
          image: ghcr.io/chentianxiong123/mybilibili-web:latest
          imagePullPolicy: Always
          env:
            - {name: NITRO_PORT, value: "3200"}
          ports: [{containerPort: 3200}]
---
apiVersion: v1
kind: Service
metadata: {name: web, namespace: mybilibili}
spec:
  selector: {app: web}
  ports:
    - {name: http, port: 3200, targetPort: 3200}
---
apiVersion: apps/v1
kind: Deployment
metadata: {name: admin, namespace: mybilibili}
spec:
  replicas: 1
  selector: {matchLabels: {app: admin}}
  template:
    metadata: {labels: {app: admin}}
    spec:
      containers:
        - name: admin
          image: ghcr.io/chentianxiong123/mybilibili-admin:latest
          imagePullPolicy: Always
          env:
            - {name: CORE_ADDR, value: "core:8080"}
            - {name: AI_ADDR, value: "ai:8088"}
          ports: [{containerPort: 3100}]
---
apiVersion: v1
kind: Service
metadata: {name: admin, namespace: mybilibili}
spec:
  selector: {app: admin}
  ports:
    - {name: http, port: 3100, targetPort: 3100}