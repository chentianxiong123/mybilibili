import 'package:flutter/material.dart';

class Style {
  static const double aspectRatio16x9 = 16.0 / 9.0;
  static const BorderRadius mdRadius = BorderRadius.all(Radius.circular(8));
  static const EdgeInsets smPadding = EdgeInsets.all(8);
  static const EdgeInsets mdPadding = EdgeInsets.all(16);
}