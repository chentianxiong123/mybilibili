import 'package:flutter/material.dart';
import 'package:mybilibili_app_flutter/piliplus/models/common/sponsor_block/post_segment_model.dart';

class PostPanel {
  static Widget segmentWidget(
    ThemeData theme, {
    required PostSegmentModel item,
    int? currentPos,
    int? videoDuration,
  }) => const SizedBox.shrink();
}