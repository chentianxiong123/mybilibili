<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybilibili.web.mapper.DynamicCommentMapper">

    <resultMap id="BaseResultMap" type="com.mybilibili.common.entity.DynamicComment">
        <id column="id" property="id"/>
        <result column="dynamic_id" property="dynamicId"/>
        <result column="user_id" property="userId"/>
        <result column="content" property="content"/>
        <result column="parent_id" property="parentId"/>
        <result column="reply_user_id" property="replyUserId"/>
        <result column="like_count" property="likeCount"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dynamic_comments (dynamic_id, user_id, content, parent_id, reply_user_id, like_count, status, created_at)
        VALUES (#{dynamicId}, #{userId}, #{content}, #{parentId}, #{replyUserId}, 0, 0, NOW())
    </insert>

    <delete id="delete">
        UPDATE dynamic_comments SET status = 1 WHERE id = #{id}
    </delete>

    <select id="getById" resultMap="BaseResultMap">
        SELECT * FROM dynamic_comments WHERE id = #{id} AND status = 0
    </select>

    <select id="getByDynamicId" resultMap="BaseResultMap">
        SELECT * FROM dynamic_comments 
        WHERE dynamic_id = #{dynamicId} AND status = 0 AND parent_id IS NULL
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByDynamicId" resultType="int">
        SELECT COUNT(*) FROM dynamic_comments 
        WHERE dynamic_id = #{dynamicId} AND status = 0
    </select>

    <update id="updateLikeCount">
        UPDATE dynamic_comments SET like_count = #{count} WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE dynamic_comments SET status = #{status} WHERE id = #{id}
    </update>

    <select id="getRepliesByParentId" resultMap="BaseResultMap">
        SELECT * FROM dynamic_comments 
        WHERE parent_id = #{parentId} AND status = 0
        ORDER BY created_at ASC
    </select>

</mapper>
