<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybilibili.web.mapper.DynamicShareMapper">

    <resultMap id="BaseResultMap" type="com.mybilibili.common.entity.DynamicShare">
        <id column="id" property="id"/>
        <result column="dynamic_id" property="dynamicId"/>
        <result column="user_id" property="userId"/>
        <result column="content" property="content"/>
        <result column="created_at" property="createdAt"/>
        <result column="status" property="status"/>
    </resultMap>

    <!-- 插入转发记录 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dynamic_shares (dynamic_id, user_id, content, created_at, status)
        VALUES (#{dynamicId}, #{userId}, #{content}, NOW(), 0)
    </insert>

    <!-- 根据动态ID查询转发列表 -->
    <select id="findByDynamicId" resultMap="BaseResultMap">
        SELECT * FROM dynamic_shares
        WHERE dynamic_id = #{dynamicId} AND status = 0
        ORDER BY created_at DESC
    </select>

    <!-- 根据用户ID查询转发列表 -->
    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT * FROM dynamic_shares
        WHERE user_id = #{userId} AND status = 0
        ORDER BY created_at DESC
    </select>

    <!-- 检查用户是否已转发该动态 -->
    <select id="findByDynamicAndUser" resultMap="BaseResultMap">
        SELECT * FROM dynamic_shares
        WHERE dynamic_id = #{dynamicId} AND user_id = #{userId} AND status = 0
        LIMIT 1
    </select>

    <!-- 删除转发记录（软删除） -->
    <update id="deleteById">
        UPDATE dynamic_shares SET status = 1 WHERE id = #{id}
    </update>

    <!-- 统计动态的转发数 -->
    <select id="countByDynamicId" resultType="int">
        SELECT COUNT(*) FROM dynamic_shares
        WHERE dynamic_id = #{dynamicId} AND status = 0
    </select>

</mapper>
