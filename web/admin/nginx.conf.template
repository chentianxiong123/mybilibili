server {
    listen 3100;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location /admin/ {
        try_files $uri $uri/ /index.html;
    }
    location = /admin { return 301 /admin/; }

    # API 请求由 traefik IngressRoute 路由，admin nginx 不做反代
}
