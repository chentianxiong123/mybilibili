# 前后端联调规划文档

## 1. 文档概述

本文档旨在为 mybilibili 项目的前后端联调提供详细的规划和指导，确保联调过程顺利进行，提高开发效率和系统质量。

## 2. 联调目标

- 验证前后端API接口的正确性和一致性
- 确保核心功能的正常运行
- 发现并解决联调过程中的问题
- 为后续的系统测试和上线做好准备

## 3. 技术栈说明

### 3.1 后端技术栈
- Spring Boot 2.7.18
- MyBatis
- MySQL 8.0
- JWT 认证
- Swagger 文档
- Redis（缓存）

### 3.2 前端技术栈
- Vue 3
- Element Plus
- Axios
- Vue Router
- Pinia
- ArtPlayer（视频播放器）

## 4. 联调准备工作

### 4.1 环境准备

| 环境 | 配置要求 | 状态 |
|------|----------|------|
| MySQL | 8.0+，创建 mybilibili 数据库 | 待确认 |
| Redis | 6.0+ | 待确认 |
| JDK | 17+ | 待确认 |
| Node.js | 16.0+ | 待确认 |
| Maven | 3.6+ | 待确认 |
| Git | 2.0+ | 待确认 |

### 4.2 服务启动

| 服务 | 启动命令 | 端口 | 状态 |
|------|----------|------|------|
| 后端服务 | `mvn spring-boot:run` | 7071 | 待启动 |
| 前端服务 | `npm run dev` | 5173 | 待启动 |

### 4.3 文档准备

- [x] API 接口文档（Swagger UI）
- [x] 数据库设计文档
- [x] 前端API调用文档
- [ ] 联调测试用例

## 5. 详细联调流程

### 5.1 阶段一：基础功能联调

#### 5.1.1 用户模块

| 功能 | 前端API调用 | 后端接口 | 联调要点 |
|------|-------------|----------|----------|
| 用户注册 | `userApi.register(userData)` | `POST /user/register` | 参数验证、密码加密、用户创建 |
| 用户登录 | `userApi.login(username, password)` | `POST /user/login` | 登录验证、JWT令牌生成 |
| 获取用户信息 | `userApi.getUserById(id)` | `GET /user/{id}` | JWT验证、用户信息返回 |
| 更新用户信息 | `userApi.updateUser(id, userData)` | `PUT /user/{id}` | 权限验证、数据更新 |

#### 5.1.2 视频模块

| 功能 | 前端API调用 | 后端接口 | 联调要点 |
|------|-------------|----------|----------|
| 视频上传 | `videoApi.uploadVideo(formData)` | `POST /video/upload` | 文件上传、视频处理 |
| 视频列表 | `videoApi.getVideoList(params)` | `GET /video/list` | 分页查询、参数验证 |
| 视频详情 | `videoApi.getVideoById(id)` | `GET /video/{id}` | 视频信息返回 |
| 视频删除 | `videoApi.deleteVideo(id)` | `DELETE /video/{id}` | 权限验证、视频删除 |

#### 5.1.3 社交互动模块

| 功能 | 前端API调用 | 后端接口 | 联调要点 |
|------|-------------|----------|----------|
| 关注用户 | `followApi.follow(userId)` | `POST /follow` | 关注关系创建 |
| 取消关注 | `followApi.unfollow(userId)` | `DELETE /follow/{userId}` | 关注关系删除 |
| 点赞视频 | `likeApi.likeVideo(videoId)` | `POST /like/video` | 点赞记录创建 |
| 评论视频 | `commentApi.addComment(commentData)` | `POST /comment` | 评论创建、权限验证 |

### 5.2 阶段二：高级功能联调

#### 5.2.1 弹幕系统

| 功能 | 前端API调用 | 后端接口 | 联调要点 |
|------|-------------|----------|----------|
| 发送弹幕 | `danmakuApi.sendDanmaku(danmakuData)` | `POST /danmaku` | 弹幕发送、存储 |
| 获取弹幕 | `danmakuApi.getDanmaku(videoId)` | `GET /danmaku/{videoId}` | 弹幕查询、格式处理 |

#### 5.2.2 搜索功能

| 功能 | 前端API调用 | 后端接口 | 联调要点 |
|------|-------------|----------|----------|
| 视频搜索 | `searchApi.searchVideo(keyword)` | `GET /search/video` | 关键词搜索、结果返回 |

#### 5.2.3 通知系统

| 功能 | 前端API调用 | 后端接口 | 联调要点 |
|------|-------------|----------|----------|
| 获取通知 | `notificationApi.getNotificationList()` | `GET /notification` | 通知查询、分页 |
| 标记已读 | `notificationApi.markAsRead(notificationId)` | `PUT /notification/{id}/read` | 状态更新 |

### 5.3 阶段三：系统集成测试

- 完整业务流程测试
- 边界情况测试
- 异常处理测试
- 性能测试

## 6. 测试计划

### 6.1 测试工具

- **Postman**：API接口测试
- **浏览器开发者工具**：前端调试
- **Swagger UI**：API文档和测试
- **Jmeter**：性能测试

### 6.2 测试用例

#### 6.2.1 用户注册测试

| 测试场景 | 输入数据 | 预期结果 |
|----------|----------|----------|
| 正常注册 | 有效的用户名、密码、邮箱 | 注册成功，返回用户信息 |
| 用户名已存在 | 已存在的用户名 | 注册失败，提示用户名已存在 |
| 密码格式错误 | 密码长度不足 | 注册失败，提示密码格式错误 |

#### 6.2.2 用户登录测试

| 测试场景 | 输入数据 | 预期结果 |
|----------|----------|----------|
| 正常登录 | 正确的用户名和密码 | 登录成功，返回用户信息和令牌 |
| 用户名错误 | 不存在的用户名 | 登录失败，提示用户名或密码错误 |
| 密码错误 | 正确的用户名，错误的密码 | 登录失败，提示用户名或密码错误 |

#### 6.2.3 视频上传测试

| 测试场景 | 输入数据 | 预期结果 |
|----------|----------|----------|
| 正常上传 | 有效的视频文件 | 上传成功，返回视频信息 |
| 文件过大 | 超过限制大小的视频文件 | 上传失败，提示文件过大 |
| 文件格式错误 | 非视频格式文件 | 上传失败，提示文件格式错误 |

## 7. 常见问题与解决方案

### 7.1 跨域问题

- **问题**：前端请求后端API时出现跨域错误
- **解决方案**：
  - 后端添加CORS配置
  - 前端使用代理配置

### 7.2 API路径不匹配

- **问题**：前端调用的API路径与后端定义的路径不一致
- **解决方案**：
  - 统一API路径命名规范
  - 使用Swagger文档作为参考

### 7.3 参数格式错误

- **问题**：前端传递的参数格式与后端期望的格式不一致
- **解决方案**：
  - 统一参数命名和格式
  - 使用DTO（数据传输对象）规范参数结构

### 7.4 响应格式不匹配

- **问题**：后端返回的响应格式与前端期望的格式不一致
- **解决方案**：
  - 统一响应格式，使用Result类封装
  - 前端响应拦截器处理统一格式

### 7.5 令牌验证失败

- **问题**：前端传递的JWT令牌验证失败
- **解决方案**：
  - 检查令牌生成和解析逻辑
  - 确认令牌在请求头中的传递方式

## 8. 联调时间安排

| 阶段 | 时间安排 | 主要任务 |
|------|----------|----------|
| 准备阶段 | 第1天 | 环境搭建、服务启动、文档准备 |
| 基础功能联调 | 第2-3天 | 用户模块、视频模块、社交互动模块联调 |
| 高级功能联调 | 第4-5天 | 弹幕系统、搜索功能、通知系统联调 |
| 系统集成测试 | 第6天 | 完整业务流程测试、边界情况测试、异常处理测试 |
| 问题修复 | 第7天 | 解决联调过程中发现的问题 |
| 验收测试 | 第8天 | 最终验收测试，确保系统正常运行 |

## 9. 风险评估

| 风险 | 影响程度 | 可能性 | 应对措施 |
|------|----------|--------|----------|
| 环境配置问题 | 高 | 中 | 提前准备环境，确保配置正确 |
| API接口不一致 | 高 | 中 | 统一API设计规范，使用Swagger文档 |
| 数据格式错误 | 中 | 高 | 严格的数据验证和测试 |
| 性能问题 | 中 | 低 | 进行性能测试，优化关键路径 |
| 安全问题 | 高 | 低 | 进行安全测试，确保系统安全 |

## 10. 责任分工

| 角色 | 职责 |
|------|------|
| 后端开发 | 提供API接口，解决后端问题 |
| 前端开发 | 调用API接口，解决前端问题 |
| 测试人员 | 执行测试用例，记录问题 |
| 项目经理 | 协调联调过程，解决冲突 |

## 11. 联调输出

- 联调测试报告
- 问题修复记录
- 优化建议
- 系统运行状态报告

## 12. 总结

前后端联调是系统开发中的关键环节，需要前后端开发人员密切配合。通过遵循本规划文档的流程和要求，可以确保联调过程顺利进行，及时发现和解决问题，提高系统的质量和开发效率。

联调完成后，系统将具备完整的功能，为后续的系统测试和上线做好准备。